# Nexus Mods OAuth Integration Credentials
# For official builds, these are injected during CI/CD.
# For local custom development, you can create a .env file based on this template.
NEXUS_CLIENT_ID=
NEXUS_CLIENT_SECRET=
NEXUS_REDIRECT_URI=palmodmanager://oauth/callback
